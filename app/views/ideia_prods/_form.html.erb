<%= form_for(@ideia_prod) do |f| %>
<% if @ideia_prod.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@ideia_prod.errors.count, "error") %> prohibited this ideia_prod from being saved:</h2>

	<ul>
		<% @ideia_prod.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="controls">
	<%= f.label :nome %>
	<%= f.text_field :nome %>
</div>
<div class="controls controls-row">
	<%= f.label :email %>
	<%= f.label :telefone %>
	<%= f.email_field :email %>
	<%= f.telephone_field :telefone %>
</div>
<div class="controls controls-row">
	<%= f.label :cidade %>
	<%= f.text_field :cidade %>
	<%= f.label :uf %>
	<%= f.text_field :uf %>
</div>
<div>
	<%= f.text_area :descricao, :cols => "150", :rows => "5", :placeholder=>"Descrião" %>
</div>

<div class="actions">
	<%= f.submit 'Enviar Mensagem',  :class=>"btn btn-primary" %>
</div>
<% end %>
